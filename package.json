{
  "name": "automated-testing",
  "version": "1.0.0",
  "description": "automated software testing with playwright",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "playwright": "npx playwright test --config=playwright.config.ts --project=Firefox",
    "e2e": "npx playwright test --config=e2e.config.ts --project=Firefox",
    "visual": "npx playwright test --config=visual.config.ts --project=Firefox"
  },
  "author": "ray",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.44.1",
    "prettier": "^3.3.1"
  }
}
